services:
  admin:
    build: ./admin
    ports:
      - "5174:80"
    networks:
      - network
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    networks:
      - network

  backend:
    build: ./backend
    ports:
      - "3003:3003"
    networks:
      - network

networks:
  network:
    driver: bridge